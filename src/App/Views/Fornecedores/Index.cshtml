@model IEnumerable<App.ViewModels.FornecedorViewModel>

@{
    ViewData["Title"] = "Fornecedores";
}

<div>
    <p>
        <a asp-action="Adicionar" class="btn btn-primary">Novo</a>
    </p>

    <hr />

    <p>
        <table class="table">
            <thead class="table-dark">
                <tr>
                    <th>#</th>
                    <th>@Html.DisplayNameFor(item => item.Nome)</th>
                    <th>@Html.DisplayNameFor(item => item.Documento)</th>
                </tr>
            </thead>

            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@item.Nome</td>
                        <td>@item.Documento</td>
                    </tr>
                }
            </tbody>
        </table>
    </p>
</div>